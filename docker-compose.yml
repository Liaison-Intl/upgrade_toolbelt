version: '2'

services:
  ci:
    build:
      context: .
      dockerfile: Dockerfile.upgrade_analyzer
    command: /opt/ci/bin/upgrade_analyzer --listen
    env_file:
      - ./kv.conf
    volumes:
      - .:/opt/ci

